name: Build Mod

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 6.0.x

    - name: Build Mod
      # This command forces the build to ignore the game path
      run: dotnet build --configuration Release /p:GamePath="." /p:EnableModDeploy=false

    - name: Upload Artifact
      uses: actions/upload-artifact@v4
      with:
        name: LeftyMode-DLL
        path: bin/Release/net6.0/
        
